{
  "schema_version": 1,
  "description": "Hand-maintained inputs for tools/generate_validation_matrix.py. Everything a runner already declares (which placements and quantization formats it supports) is read from its LoadDeclaration and must not be repeated here. This file holds only what cannot be derived: what each hardware profile can attempt, how many ranks a placement gets, and which models need an input this harness cannot supply.",
  "world_size_policy": {
    "eager": 1,
    "replicated": 4,
    "fsdp_blockwise": 4
  },
  "world_size_rationale": "One rank for eager because it shards nothing, which a screening plan case may override when it needs eager at the rank count the model is served on. Measured on MI355X: host memory is a fixed cost per rank, 18.7G for eager and 8.5G for blockwise fill, identical at four and eight ranks. Blockwise fill's share does not fall as the shard gets smaller because what is left on the host is the process itself rather than the weights, so the gap between the two widens with every rank added. The multi-rank default is deliberately conservative for profiles whose machines are small; a profile whose hardware ships more devices should override it, because testing a rank count nobody deploys measures a configuration that does not exist. Profiles override this with their own world_size_policy.",
  "profiles": [
    {
      "id": "mi3xx",
      "backend": "rocm_torchao",
      "accelerator": "gfx942_or_gfx950",
      "transformers": "5.x",
      "tags": ["rocm", "torchao", "generated"],
      "world_size_policy": {
        "eager": 1,
        "replicated": 8,
        "fsdp_blockwise": 8
      },
      "world_size_note": "Eight ranks because an MI300X or MI355X node ships eight devices and that is how these models are actually served, so a four-rank result would describe a deployment nobody runs.",
      "runtime_args": ["--attention_backend", "aiter", "--use_torch_compile"],
      "runtime_args_note": "Taken from .ci/benchmark_configs, where every eight-GPU Instinct entry pins attention_backend: aiter and use_torch_compile: true. Both were previously left unset: attention selection happened to resolve to AITER anyway, so the sweep agreed with production by accident and would have drifted silently if the selector changed, while compile was simply never exercised even though nothing is served without it.",
      "screening_plan": {
        "rationale": "A screening set, not a full cross. Crossing placement by transformer quantization by text-encoder quantization is twelve cases per model, and running it for every model spends most of its time re-confirming that the same loading machinery works. These four cover every level of every factor, deliberately loading two of them densely: if a dense case fails, the model is expanded into the unconfounded cross to localize it. The bf16 sharded case is kept unconfounded on purpose, so a failure in the dense sharded case can be attributed by comparing the two rather than by re-running.",
        "cases": [
          {
            "placement": "eager",
            "quantization": "none",
            "te_fp8": false,
            "role": "reference for the image comparison, and the cheapest proof the model loads at all"
          },
          {
            "placement": "eager",
            "quantization": "none",
            "te_fp8": false,
            "world_size": 8,
            "role": "the baseline the sharded cases are measured against: eight ranks, each holding a full copy, which is what you would run on this node without memory-efficient loading. Measured: at eight ranks it costs 149.3G of host memory and 23.9G of load VRAM against blockwise fill's 68.2G and 13.9G, and loads 17% slower. The one-rank eager case above is the image to compare against, not a baseline for cost, because comparing an eight-rank run to a one-rank run confounds the loader with the parallelism."
          },
          {
            "placement": "fsdp_blockwise",
            "quantization": "none",
            "te_fp8": false,
            "role": "the blockwise disk fill on its own, with nothing quantized to confuse a failure"
          },
          {
            "placement": "fsdp_eager_fill",
            "quantization": "none",
            "te_fp8": false,
            "role": "control for the case above: same ranks and same FSDP sharding, materialized in full first, so the memory difference between the two is the blockwise fill rather than the rank count"
          },
          {
            "placement": "fsdp_blockwise",
            "quantization": "fp8",
            "te_fp8": true,
            "role": "dense: blockwise fill, per-block quantize and a quantized text encoder together"
          },
          {
            "placement": "replicated",
            "quantization": "none",
            "te_fp8": false,
            "role": "the broadcast fill on its own, so the dense replicated case below splits into what the broadcast costs and what quantizing costs. Its load VRAM is eager's by construction, since every rank still ends up with a full copy; the host figure is what this placement is for, and without this case the only unquantized measurement of it is a four-rank curated pair."
          },
          {
            "placement": "replicated",
            "quantization": "fp8",
            "te_fp8": true,
            "role": "dense: broadcast fill with both quantizations, the path that has no per-block fill"
          }
        ]
      },
      "quantizations": ["none", "fp8"],
      "quantizations_withheld": {
        "int8": "TorchAO INT8 has no ROCm kernel path, so every case would be a rejection. Rejections stay curated: their value is a human asserting an external fact, which a generator reading the same probe code cannot do.",
        "fp4": "The outcome differs across the two archs this token covers, since AITER ships no FP4 kernels for gfx942 but does for gfx950, so an FP4 case cannot share the pair token. The gfx950 FP4 cases are curated and pinned.",
        "fp8_fp4": "Needs the FP4 half, so it inherits the same split."
      }
    }
  ],
  "requires_input_image": {
    "reason": "These runners need a real input image or video, so a case that passes none would fail for reasons unrelated to loading. A model here is generated as soon as its sampling entry in curated_cases.json names an input image, which is the file its own benchmark config passes, reached through an environment placeholder so a machine without that file reports an environment mismatch instead of a failure. The models left in this list with no such entry get no generated case, and any coverage for them stays curated.",
    "models": [
      "CausalWan",
      "FLUX.1-Kontext-dev",
      "Hunyuanvideo-1.5",
      "Hunyuanvideo-1.5-Distilled",
      "Hunyuanvideo-1.5-Sparse",
      "LTX-2",
      "LTX-2.3",
      "MiniMax-H3-Ref2VA",
      "Qwen-Image-Edit",
      "Wan2.1-I2V",
      "Wan2.1-VACE-1.3B",
      "Wan2.1-VACE-14B",
      "Wan2.2-Distilled-I2V",
      "Wan2.2-I2V",
      "Wan2.2-TI2V"
    ]
  }
}
